services:
  mitmproxy:
    image: mitmproxy/mitmproxy:latest
    command: mitmweb --web-host 0.0.0.0 --listen-host 0.0.0.0 --listen-port 8080 --web-port 8081
    ports:
      - "8080:8080"
      - "8081:8081"
    restart: unless-stopped
