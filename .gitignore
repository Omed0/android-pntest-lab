node_modules/
bun.lock
.env
.env.*

# Downloaded frida binaries (large)
tools/frida/
tools/cache/

# Android SDK if installed locally inside the lab
tools/android-sdk/

# APKs under test
*.apk

# Local Burp certificates
cert/*.cer
cert/*.crt
cert/*.der
cert/*.pem

# Disposable verification workspaces (may contain extracted/pulled APK
# content — never committed, see README "Safety")
testing/

.claude/