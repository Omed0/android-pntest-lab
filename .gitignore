node_modules/
bun.lock
.env
.env.*

# Downloaded frida binaries (large)
tools/frida/
tools/cache/

# Android SDK if installed locally inside the lab
tools/android-sdk/

# APKs under test
*.apk

# Local Burp certificates
cert/*.cer
cert/*.crt
cert/*.der
cert/*.pem

# Disposable verification workspaces (may contain extracted/pulled APK
# content — never committed, see README "Safety")
testing/

# Regenerated on every `bun run.ts` from the live proxy CA + host/port —
# see src/unpinning.ts. Never hand-edit; edit scripts/unpinning/config.js's
# template values instead, or pass different --proxy-host/--proxy-port flags.
scripts/unpinning/config.generated.js

.claude/